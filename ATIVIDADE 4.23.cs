using System;


class Program
{
  static void Main()
  {
    Console.Write("Digite um número: ");
    float num1 = float.Parse(Console.ReadLine());
    
    resultado = num1 > 0 ? "Positivo" : (num1 < 0 "Negativo" : "Zero");;
  }
  
}